import { View, Text } from 'react-native';
import React from 'react';

type Props = {};

const CourierSettings = (props: Props) => {
    return (
        <View>
            <Text>CourierSettings</Text>
        </View>
    );
};

export default CourierSettings;
